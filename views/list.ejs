<%- include("header") -%>
    <div class="box" id="heading">
        <h1 style="color: purple;"><%= ListTitle %></h1>
    </div>

    <div class="box">
        <form class="item" action="/<%= Token %>" method="POST">
            <input type="text" name="Items" placeholder="Input Items" />
            <button type="submit" name="list" value="<%= ListTitle %>">+</button>
        </form>
            
        <!--li> <%= newListItem %> </li-->
        <% newListItem.forEach(el => { %> 
            <form action="/delete/<%= Token %>" method="POST">
                <div class="item">
                    <input type="checkbox" name="checkbox" value="<%= el %>" onchange="this.form.submit()">
                    <p><%= el %></p>
                </div>
            </form>
        <% }); %>    
    </div>
<%- include("footer") -%>